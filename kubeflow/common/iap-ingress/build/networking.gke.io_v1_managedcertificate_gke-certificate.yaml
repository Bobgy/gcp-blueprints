apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  labels:
    kustomize.component: iap-ingress
  name: gke-certificate
  namespace: istio-system
spec:
  domains:
  - KUBEFLOW-NAME.endpoints.PROJECT.cloud.goog
